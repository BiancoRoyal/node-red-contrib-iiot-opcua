name: Ren a build
on:
  push:
    branches:
      - main
      - development
      - add-github-actions
jobs:
  build:
    runs-on: ubuntu-20.04
    strategy:
      matrix:
        node: [14]
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node }}
      - run: npm install
      - run: npm run build
      - run: tar -czvf distrib.tar.gz opcuaIIoT examples package.json create_certificates.js
      - uses: actions/upload-artifact@v3
        with:
          name: build-folders
          path: distrib.tar.gz

  publish:
    runs-on: ubuntu-20.04
    needs: build
    steps:
      - uses: actions/download-artifact@v3
        with: # Download artifacts from build.yml hopefully
          name: build-folders
          path: distrib.tar.gz
      - run: printenv
      - run: file distrib.tar.gz
      - run: tar -xvf distrib.tar.gz
      - run: pwd
      - run: ls
      - run: file opcuaIIoT
